syntax = "proto3";

package meshtastic;

import "nanopb.proto";

message CrisislabMessage {
	oneof message {
		uint32 broadcast_interval_seconds = 1;
		string channel_name = 2 [(nanopb).max_length = 12];
	}
}
