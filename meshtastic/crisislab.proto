syntax = "proto3";

package meshtastic;

import "nanopb.proto";

message CrisislabMessage {
	oneof message {
		Command command = 1;
	}

	message Command {
		oneof payload {
			uint32 broadcast_interval_seconds = 2;
			string channel_name = 3 [(nanopb).max_length = 12];
		}
	}
}
